#include <stdio.h>

int main(void) {
       int a = 999;

       printf("%zu\n", sizeof a);

       printf("%zu\n", sizeof 3.14);

      //To print negative values use %zd
      //Returns values in type size_t
      //Size_of is entirely a compile-time operation.

}

