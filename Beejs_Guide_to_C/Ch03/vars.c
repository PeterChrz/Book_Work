#include <stdio.h> 

int main(void)
{
    int i = 2;
    float f = 3.14;
    char *s = "Hello World!"; // char * ("Char pointer") is the string type.

    printf("%s i = %d and f = %f!\n", s, i, f); //Order of vars doesn't matter but is reccomended for readability.

}
